# HR Performance Management - Local Development Environment
# 
# LOCATION: /deploy/.env.example (committed to repo as template)
# USAGE: Copy to /deploy/.env and fill in values
# WARNING: Never commit .env files with real credentials
#
# Repository: https://github.com/Autonom664/HR (private)

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# MongoDB credentials (required)
MONGO_ROOT_USERNAME=hrapp_dev
MONGO_ROOT_PASSWORD=change_this_password_in_production

# Database name
DB_NAME=hr_performance

# =============================================================================
# URL CONFIGURATION
# =============================================================================
# Backend API URL - how frontend reaches the backend
#
# OPTIONS:
# 1. Same-origin (recommended for production): Leave EMPTY or don't set
#    Frontend will use relative '/api' paths, nginx proxies to backend
#    REACT_APP_BACKEND_URL=
#
# 2. Cross-origin (for local dev without nginx): Use full URL
#    REACT_APP_BACKEND_URL=http://localhost:8001
#
# For local development with docker-compose (no nginx):
REACT_APP_BACKEND_URL=http://localhost:8001

# CORS allowed origins (comma-separated, no spaces)
# Must match the frontend URL(s) that will access the API
CORS_ORIGINS=http://localhost:3000

# =============================================================================
# PORT CONFIGURATION
# These ports bind to localhost only in staging/production
# =============================================================================
BACKEND_PORT=8001
FRONTEND_PORT=3000

# =============================================================================
# AUTHENTICATION CONFIGURATION
# =============================================================================
# Auth mode: 'password' (default) or 'entra' (Microsoft Entra ID)
# Password mode: Users receive generated passwords via admin CSV export
# Entra mode: Microsoft Entra ID SSO (scaffolded, enable when ready)
AUTH_MODE=password

# Session expiry in hours
SESSION_EXPIRY_HOURS=24

# =============================================================================
# COOKIE SECURITY
# =============================================================================
# Set to 'false' for local dev without HTTPS
# SECURITY: MUST be 'true' in staging/production with HTTPS
COOKIE_SECURE=false
COOKIE_SAMESITE=lax

# =============================================================================
# ENTRA SSO CONFIGURATION (scaffolded, not enabled)
# =============================================================================
# Leave empty to use email authentication
# To enable: set AUTH_MODE=entra and fill in these values
ENTRA_TENANT_ID=
ENTRA_CLIENT_ID=
ENTRA_CLIENT_SECRET=
ENTRA_REDIRECT_URI=
ENTRA_SCOPES=openid profile email
